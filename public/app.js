const $=s=>document.querySelector(s),K='forgecodex_settings_v3';
let hist=[];
const FREE_MODELS={
  'openrouter/free':'Auto Free',
  'z-ai/glm-5.2:free':'GLM-5.2 Free',
  'nvidia/nemotron-3-ultra-550b-a55b:free':'Nemotron 3 Ultra Free',
  'minimax/minimax-m2.7:free':'MiniMax M2.7 Free',
  'thinkingmachines/inkling:free':'Inkling Free',
  'dots-studio/dots-3-note-preview:free':'Dots3 Note Free'
};
function settings(){try{return JSON.parse(localStorage.getItem(K)||localStorage.getItem('forgecodex_settings_v2')||'{}')}catch{return{}}}
function putSettings(p){const k={...settings(),...p};localStorage.setItem(K,JSON.stringify(k));return k}
function selectedModel(){const choice=$('#model')?.value||settings().modelChoice||settings().model||'openrouter/free';if(choice==='__custom__')return ($('#customModel')?.value||settings().customModel||'').trim();return choice}
function webMode(){return $('#webMode')?.value||settings().webMode||'auto'}
function webLabel(){return webMode()==='auto'?'Web Auto':webMode()==='always'?'Web On':''}
function saveSettings(){const modelChoice=$('#model').value;const customModel=$('#customModel').value.trim();if(modelChoice==='__custom__'&&!customModel)throw Error('Custom model IDを入力してください');putSettings({or:$('#or').value.trim(),modelChoice,customModel,model:selectedModel(),webMode:webMode(),clientId:$('#clientId').value.trim()});setStatus('Settings saved · '+modeLabel());if(settings().ghToken)loadRepos().catch(showErr)}
function setStatus(t){$('#status').textContent=t}
function showErr(e){setStatus('Error');add('err','Error: '+(e?.message||e))}
function add(c,t){const x=document.createElement('div');x.className='msg '+c;const s=String(t??'');const re=/(https?:\/\/[^\s<>]+)/g;let last=0,m;while((m=re.exec(s))){if(m.index>last)x.append(document.createTextNode(s.slice(last,m.index)));const a=document.createElement('a');a.href=m[0].replace(/[),.;]+$/,'');a.textContent=a.href;a.target='_blank';a.rel='noopener noreferrer';x.append(a);const tail=m[0].slice(a.href.length);if(tail)x.append(document.createTextNode(tail));last=m.index+m[0].length}if(last<s.length)x.append(document.createTextNode(s.slice(last)));$('#chat').append(x);x.scrollIntoView({block:'end'})}
function sleep(ms){return new Promise(r=>setTimeout(r,ms))}
function hasGitHub(){return !!settings().ghToken}
function modelLabel(){const m=selectedModel()||settings().model||'openrouter/free';return FREE_MODELS[m]||m}
function modeLabel(){const w=webLabel();return (hasGitHub()?'GitHub Agent · ':'Chat · ')+modelLabel()+(w?' · '+w:'')}
function updateModelUI(){const custom=$('#model').value==='__custom__';$('#customModel').classList.toggle('hidden',!custom);putSettings({modelChoice:$('#model').value,customModel:$('#customModel').value.trim(),model:selectedModel()});setStatus(modeLabel())}
async function gh(path,opt={}){const k=settings();if(!k.ghToken)throw Error('GitHubにサインインしてください');const h={Accept:'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28',Authorization:`Bearer ${k.ghToken}`,...(opt.headers||{})};if(opt.body)h['Content-Type']='application/json';const r=await fetch('https://api.github.com'+path,{...opt,headers:h});const t=await r.text();let d;try{d=t?JSON.parse(t):null}catch{d=t}if(!r.ok){if(r.status===401){putSettings({ghToken:''});updateGitHubUI()}throw Error(d?.message||`GitHub ${r.status}`)}return d}
async function githubLogin(){const clientId=$('#clientId').value.trim();if(!clientId)throw Error('GitHub OAuth Client IDを入力してください');putSettings({clientId});setStatus('GitHub認証を開始…');const body=new URLSearchParams({client_id:clientId,scope:'repo read:user'});const r=await fetch('https://github.com/login/device/code',{method:'POST',headers:{Accept:'application/json','Content-Type':'application/x-www-form-urlencoded'},body});const d=await r.json();if(!r.ok||d.error)throw Error(d.error_description||d.error||`GitHub ${r.status}`);$('#deviceCode').textContent=d.user_code;$('#deviceLink').href=d.verification_uri||'https://github.com/login/device';$('#deviceBox').classList.remove('hidden');window.open(d.verification_uri||'https://github.com/login/device','_blank','noopener');let interval=Math.max(5,Number(d.interval)||5);const end=Date.now()+(Number(d.expires_in)||900)*1000;while(Date.now()<end){await sleep(interval*1000);setStatus('GitHubの認証待ち…');const p=new URLSearchParams({client_id:clientId,device_code:d.device_code,grant_type:'urn:ietf:params:oauth:grant-type:device_code'});const tr=await fetch('https://github.com/login/oauth/access_token',{method:'POST',headers:{Accept:'application/json','Content-Type':'application/x-www-form-urlencoded'},body:p});const td=await tr.json();if(td.access_token){putSettings({ghToken:td.access_token});$('#deviceBox').classList.add('hidden');await updateGitHubUI();await loadRepos();setStatus(modeLabel());return}if(td.error==='authorization_pending')continue;if(td.error==='slow_down'){interval+=5;continue}throw Error(td.error_description||td.error||'GitHub authentication failed')}throw Error('GitHub認証コードの有効期限が切れました')}
async function githubLogout(){putSettings({ghToken:''});$('#repo').innerHTML='';$('#branch').innerHTML='';await updateGitHubUI();setStatus(modeLabel())}
async function updateGitHubUI(){const k=settings();const off=!k.ghToken;$('#repo').disabled=off;$('#branch').disabled=off;$('#reload').disabled=off;['open','commit','branchCreate','prCreate'].forEach(id=>{const el=$('#'+id);if(el)el.disabled=off});if(off){$('#ghUser').textContent='GitHub: 未ログイン（チャットは利用可能）';$('#ghLogin').classList.remove('hidden');$('#ghLogout').classList.add('hidden');return}try{const u=await gh('/user');$('#ghUser').textContent='GitHub: @'+u.login+' · Agent有効';$('#ghLogin').classList.add('hidden');$('#ghLogout').classList.remove('hidden')}catch{$('#ghUser').textContent='GitHub: 再ログインが必要（チャットは利用可能）';$('#ghLogin').classList.remove('hidden');$('#ghLogout').classList.add('hidden')}}
async function orChat(messages,toolsList){const k=settings();if(!k.or)throw Error('OpenRouter key required');const model=selectedModel()||k.model||'openrouter/free';if(!model)throw Error('Model ID required');const body={model,messages};const allTools=[...(toolsList||[])];if(webMode()==='auto'){allTools.push({type:'openrouter:web_search',parameters:{engine:'parallel',max_results:5,max_total_results:10,search_context_size:'low'}},{type:'openrouter:web_fetch',parameters:{engine:'openrouter',max_content_tokens:12000}})}else if(webMode()==='always'){body.plugins=[{id:'web',engine:'parallel',mode:'turbo',max_results:5}]}if(allTools.length){body.tools=allTools;body.tool_choice='auto'}if(model.startsWith('z-ai/glm-5.2'))body.reasoning={effort:'high'};const r=await fetch('https://openrouter.ai/api/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${k.or}`,'Content-Type':'application/json','HTTP-Referer':location.origin,'X-Title':'ForgeCodex'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw Error(d?.error?.message||`OpenRouter ${r.status}`);return d}
function withCitations(m){let out=m?.content||'';const anns=(m?.annotations||[]).filter(a=>a?.type==='url_citation'&&a.url_citation?.url);if(anns.length){const seen=new Set(),links=[];for(const a of anns){const u=a.url_citation.url;if(seen.has(u))continue;seen.add(u);links.push(`${a.url_citation.title||'Source'}: ${u}`)}if(links.length)out+='\n\nSources:\n'+links.join('\n')}return out}
function active(){const v=$('#repo').value||'';const i=v.indexOf('/');return{owner:v.slice(0,i),repo:v.slice(i+1),ref:$('#branch').value||'main'}}
async function loadRepos(){if(!hasGitHub())return;setStatus('Loading repos…');const d=await gh('/user/repos?per_page=100&sort=updated&affiliation=owner,collaborator,organization_member');$('#repo').innerHTML=d.map(x=>`<option>${x.full_name}</option>`).join('');await loadBranches();setStatus(modeLabel())}
async function loadBranches(){if(!hasGitHub())return;const r=active();if(!r.owner)return;const d=await gh(`/repos/${encodeURIComponent(r.owner)}/${encodeURIComponent(r.repo)}/branches?per_page=100`);$('#branch').innerHTML=d.map(x=>`<option>${x.name}</option>`).join('')}
const b64enc=s=>btoa(unescape(encodeURIComponent(s))),b64dec=s=>decodeURIComponent(escape(atob((s||'').replace(/\n/g,''))));
async function getFile(path,ref){const r=active();return gh(`/repos/${encodeURIComponent(r.owner)}/${encodeURIComponent(r.repo)}/contents/${path.split('/').map(encodeURIComponent).join('/')}?ref=${encodeURIComponent(ref||r.ref)}`)}
async function writeFile(path,content,message,branch){const r=active();let old=null;try{old=await getFile(path,branch||r.ref)}catch{}const body={message:message||`Update ${path} via ForgeCodex`,content:b64enc(content),branch:branch||r.ref};if(old?.sha)body.sha=old.sha;return gh(`/repos/${encodeURIComponent(r.owner)}/${encodeURIComponent(r.repo)}/contents/${path.split('/').map(encodeURIComponent).join('/')}`,{method:'PUT',body:JSON.stringify(body)})}
async function listTree(ref){const r=active();return gh(`/repos/${encodeURIComponent(r.owner)}/${encodeURIComponent(r.repo)}/git/trees/${encodeURIComponent(ref||r.ref)}?recursive=1`)}
async function createBranch(name,from){const r=active();const s=await gh(`/repos/${encodeURIComponent(r.owner)}/${encodeURIComponent(r.repo)}/git/ref/heads/${encodeURIComponent(from||r.ref)}`);return gh(`/repos/${encodeURIComponent(r.owner)}/${encodeURIComponent(r.repo)}/git/refs`,{method:'POST',body:JSON.stringify({ref:`refs/heads/${name}`,sha:s.object.sha})})}
async function createPR(title,head,base='main',body=''){const r=active();return gh(`/repos/${encodeURIComponent(r.owner)}/${encodeURIComponent(r.repo)}/pulls`,{method:'POST',body:JSON.stringify({title,head,base,body})})}
const tools=[
{type:'function',function:{name:'list_tree',description:'List repository files recursively',parameters:{type:'object',properties:{ref:{type:'string'}}}}},
{type:'function',function:{name:'read_file',description:'Read a repository text file',parameters:{type:'object',properties:{path:{type:'string'},ref:{type:'string'}},required:['path']}}},
{type:'function',function:{name:'write_file',description:'Create or update a text file and commit it',parameters:{type:'object',properties:{path:{type:'string'},content:{type:'string'},message:{type:'string'},branch:{type:'string'}},required:['path','content']}}},
{type:'function',function:{name:'create_branch',description:'Create a Git branch',parameters:{type:'object',properties:{branch:{type:'string'},from:{type:'string'}},required:['branch']}}},
{type:'function',function:{name:'create_pr',description:'Create a pull request',parameters:{type:'object',properties:{title:{type:'string'},head:{type:'string'},base:{type:'string'},body:{type:'string'}},required:['title','head']}}}
];
async function tool(name,a){if(name==='list_tree'){const d=await listTree(a.ref);return{tree:(d.tree||[]).filter(x=>x.type==='blob').slice(0,3000).map(x=>x.path),truncated:d.truncated}}if(name==='read_file'){const d=await getFile(a.path,a.ref);return{path:a.path,sha:d.sha,content:b64dec(d.content)}}if(name==='write_file'){const d=await writeFile(a.path,a.content,a.message,a.branch);return{commit:d.commit?.sha,content:d.content?.sha,path:a.path}}if(name==='create_branch'){const d=await createBranch(a.branch,a.from);return{ref:d.ref,sha:d.object?.sha}}if(name==='create_pr'){const d=await createPR(a.title,a.head,a.base,a.body);return{number:d.number,url:d.html_url,title:d.title}}return{error:'unknown tool'}}
async function runChat(text){setStatus(modelLabel()+' · Chat'+(webLabel()?' · '+webLabel():''));const msgs=[{role:'system',content:'You are ForgeCodex. Help with coding, debugging, architecture, explanations, planning, and general questions. GitHub is not connected, so do not claim to have inspected, changed, committed, tested, or accessed any repository. When agentic web tools are available, decide yourself whether fresh web information is actually needed. Search only when the answer depends on current, niche, uncertain, or externally verifiable information; do not search for stable knowledge you already know. When you search, cite useful sources. If repository access would materially help, say that GitHub connection would enable it, but still answer as far as possible from the user message.'},...hist,{role:'user',content:text}];const d=await orChat(msgs);const m=d.choices?.[0]?.message;if(!m)throw Error('No model response');const out=withCitations(m);hist.push({role:'user',content:text},{role:'assistant',content:m.content||out});setStatus(modeLabel());return out}
async function runGitHubAgent(text){const r=active();if(!r.owner)throw Error('GitHubリポジトリを選択してください');let msgs=[{role:'system',content:`You are ForgeCodex, a careful coding agent operating directly on GitHub from an iPad browser. Current repository: ${r.owner}/${r.repo}, branch: ${r.ref}. Inspect files before editing. Make minimal, coherent changes. For substantial changes prefer creating a branch and PR. You have no shell, terminal, build runner, filesystem outside GitHub, or test execution. Never claim a build/test ran unless evidence is present in repository data. When agentic web tools are available, decide yourself whether fresh web information is actually needed. Search for current documentation, APIs, libraries, errors, versions, or other changing/uncertain information when it materially improves the task; otherwise do not search. Use web_fetch when a search result or user-supplied URL needs full-page details. Cite useful web sources.`},...hist,{role:'user',content:text}];for(let i=0;i<12;i++){setStatus(`${modelLabel()} · Agent ${i+1}/12`+(webLabel()?' · '+webLabel():''));const d=await orChat(msgs,tools);const m=d.choices?.[0]?.message;if(!m)throw Error('No model response');msgs.push(m);if(!m.tool_calls?.length){const out=withCitations(m);hist.push({role:'user',content:text},{role:'assistant',content:m.content||out});setStatus(modeLabel());return out}for(const c of m.tool_calls){if(!c?.function?.name)continue;let a={};try{a=JSON.parse(c.function.arguments||'{}')}catch{}let out;try{out=await tool(c.function.name,a)}catch(e){out={error:e.message}}msgs.push({role:'tool',tool_call_id:c.id,name:c.function.name,content:JSON.stringify(out).slice(0,90000)})}}throw Error('Agent step limit reached')}
async function run(text){return hasGitHub()?runGitHubAgent(text):runChat(text)}
$('#save').onclick=()=>{try{saveSettings()}catch(e){showErr(e)}};$('#ghLogin').onclick=()=>githubLogin().catch(showErr);$('#ghLogout').onclick=()=>githubLogout().catch(showErr);$('#model').onchange=updateModelUI;$('#customModel').onchange=updateModelUI;$('#webMode').onchange=()=>{putSettings({webMode:webMode()});setStatus(modeLabel())};$('#repo').onchange=()=>loadBranches().catch(showErr);$('#reload').onclick=()=>loadRepos().catch(showErr);
$('#send').onclick=async()=>{const t=$('#prompt').value.trim();if(!t)return;$('#prompt').value='';add('u',t);$('#send').disabled=true;try{add('a',await run(t))}catch(e){showErr(e)}finally{$('#send').disabled=false}};
$('#clear').onclick=()=>{hist=[];$('#chat').innerHTML=''};
$('#open').onclick=async()=>{try{const d=await getFile($('#path').value.trim());$('#editor').value=b64dec(d.content);setStatus('File opened')}catch(e){showErr(e)}};
$('#commit').onclick=async()=>{try{await writeFile($('#path').value.trim(),$('#editor').value,$('#msg').value.trim());setStatus('Committed')}catch(e){showErr(e)}};
$('#branchCreate').onclick=async()=>{try{const n=$('#newBranch').value.trim();await createBranch(n);await loadBranches();$('#branch').value=n;$('#gitout').textContent='Created '+n}catch(e){showErr(e)}};
$('#prCreate').onclick=async()=>{try{const title=$('#prTitle').value.trim()||'ForgeCodex changes';const d=await createPR(title,$('#branch').value,'main','Created from ForgeCodex iPad');$('#gitout').textContent=d.html_url}catch(e){showErr(e)}};
document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');['chat','files','github'].forEach(x=>$('#'+x+'Tab').classList.toggle('hidden',x!==b.dataset.tab))});
(async()=>{const k=settings();$('#or').value=k.or||'';$('#clientId').value=k.clientId||'';$('#webMode').value=k.webMode||'auto';const known=[...$('#model').options].map(o=>o.value);let choice=k.modelChoice||k.model||'openrouter/free';if(!known.includes(choice)){k.customModel=choice;choice='__custom__'}$('#model').value=choice;$('#customModel').value=k.customModel||'';$('#customModel').classList.toggle('hidden',choice!=='__custom__');putSettings({modelChoice:choice,customModel:$('#customModel').value.trim(),model:selectedModel(),webMode:webMode()});await updateGitHubUI();if(hasGitHub())loadRepos().catch(showErr);else setStatus('Chat ready · '+modelLabel()+(webLabel()?' · '+webLabel():''))})();